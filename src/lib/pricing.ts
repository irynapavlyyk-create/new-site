// Regular PRO price as a number, for analytics payloads (Meta Pixel
// InitiateCheckout/Purchase value). Display strings live in promo.ts /
// translations.ts and the real charge is the Stripe price — keep all three in
// sync when the price changes.
export const PRO_PRICE_EUR = 9.99;
export const PRICE_CURRENCY = "EUR";
